{% for page in taxonomy.findTaxonomy({'tag':'id-footer'}) %}

	{% include 'partials/modular.html.twig' %}

	<div class="social pt-0 pb-0">

		<section class="container pt-0 pb-0">
			<div class="social-links small">
				{{ page.content|raw }}
			</div>
		</section>

	</div>
{% endfor %}
